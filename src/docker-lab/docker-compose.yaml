# docker run -it -p 1880:1880 -v node_red_data:/data --name mynodered nodered/node-red
services:
  nodered:
    image: nodered/node-red
    container_name: node_red
    ports:
      - "1880:1880"
    volumes:
      - /var/node_red_data:/data
